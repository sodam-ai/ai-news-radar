# .resume.md — AI News Radar 작업 이어하기 기록

> 마지막 업데이트: 2026-04-17
> 작성자: Claude Code (Sonnet 4.6)

---

## 프로젝트 경로

```
D:\AI_Dev_Work\2026y\26y_03m_22d_ai-news-radar\ai-news-radar\
```

## GitHub 저장소

```
https://github.com/sodam-ai/ai-news-radar
```

## 현재 브랜치

```
feat/v1.4.0-sqlite-discord-actions
```

---

## 관련 폴더 / 파일 위치

| 파일 | 경로 |
|------|------|
| 메인 대시보드 | `app.py` |
| DB 계층 (단일 진실 공급원) | `db/database.py` |
| DB 패키지 | `db/__init__.py` |
| 벡터 검색 | `ai/vector_store.py` |
| AI 채팅 | `ai/chat.py` |
| AI 배치 처리 | `ai/batch_processor.py` |
| 스마트 알림 | `ai/smart_alert.py` |
| 북마크 유틸 | `utils/bookmarks.py` |
| 텔레그램 봇 | `bot/telegram_bot.py` |
| Discord 봇 | `bot/discord_bot.py` |
| GitHub Actions | `.github/workflows/crawl.yml` |
| 마이그레이션 (1회용) | `utils/migrate_json_to_sqlite.py` |
| 환경 변수 | `.env` (직접 생성, Git 제외) |
| 패키지 목록 | `requirements.txt` |
| 설정 | `config.py` |
| 데이터 폴더 | `data/` (Git 제외, 런타임 자동 생성) |

---

## 완료된 작업 ✅

### [v1.4.0] Phase 2 — SQLite 전환 + GitHub Actions + Discord 봇

#### Phase 2-A: SQLite 전환 (articles.json 5MB → radar.db)
- [x] `db/__init__.py` — DB 패키지 초기화 (28개 공개 함수 re-export)
- [x] `db/database.py` — SQLite WAL+FTS5 전체 CRUD 계층
  - thread-local 커넥션 (Streamlit 멀티스레드 안전)
  - 7개 테이블: articles, briefings, watchlist, bookmarks, sources, alert_log, weekly_reports
  - FTS5 가상 테이블 + 3개 트리거 (insert/update/delete 자동 인덱스 동기화)
  - 모든 쿼리 `?` 플레이스홀더 (SQL Injection 방지)
  - `PRAGMA journal_mode=WAL`, `PRAGMA foreign_keys=ON`
- [x] `config.py` — `DB_PATH = DATA_DIR / "radar.db"` 추가
- [x] `utils/migrate_json_to_sqlite.py` — 1회성 마이그레이션 (3,234기사 검증 완료)
- [x] `.gitignore` — `data/radar.db`, `data/radar.db-wal`, `data/radar.db-shm` 추가
- [x] `crawler/rss_crawler.py` — articles.json → DB (`upsert_articles`, `update_source_crawled`)
- [x] `ai/batch_processor.py` — `get_unprocessed_articles()` + `update_article_fields()`
- [x] `ai/chat.py` — ChromaDB → FTS5 `search_articles()` → 키워드 fallback
- [x] `ai/smart_alert.py` — `get_active_keywords()`, `mark_alerted()`, `get_alert_log()` DB화
- [x] `utils/bookmarks.py` — `db.database` 함수 thin wrapper로 전면 교체
- [x] `bot/telegram_bot.py` — 모든 JSON 접근 → DB 호출, FTS5 검색 연동

#### Phase 2-A: app.py DB 연동 + UI/UX 개선
- [x] `safe_read/write_json` → DB imports 전환 (init_db, get_articles 등 18개 함수)
- [x] `@st.cache_data(ttl=60)` — DB 쿼리 60초 캐싱 (성능 최적화)
- [x] FTS5 `search_articles()` — Python 루프 검색 대체 (~50배 빠름)
- [x] 북마크 토글: `add_bookmark()` / `remove_bookmark()` DB 직접 호출
- [x] 읽음 표시: `update_article_fields()` DB 업데이트
- [x] 워치리스트: `add_watchlist_keyword()` / `get_active_keywords()` DB화
- [x] 사이드바 통계: `get_article_count()` + AI 처리율 프로그레스바

#### Phase 2-B: 통합 테스트 + 보안 검증
- [x] SQL Injection 테스트: PASS (3234기사 그대로)
- [x] 동시성 테스트: PASS (20 스레드 병렬 읽기)
- [x] FTS5 성능: 0.3ms/쿼리 (목표 <50ms 대비 167배 빠름)
- [x] 보안 스캔: PASS (하드코딩 비밀정보 없음)

#### Phase 2-C: GitHub Actions 자동 수집
- [x] `.github/workflows/crawl.yml` — 매시간 cron + workflow_dispatch
  - GitHub Secrets(`GEMINI_API_KEY`) 사용
  - DB 캐시 보존 (`actions/cache@v4`)
  - 크롤 완료 후 자동 커밋+푸시

#### Phase 2-D: Discord 봇
- [x] `bot/discord_bot.py` — `/top`, `/today`, `/search`, `/alert`, `/stats` 명령어
- [x] `requirements.txt` — `discord.py>=2.3.0` 추가

### [v1.3.0] ChromaDB 시맨틱 검색 + 텔레그램 봇 고도화
- [x] `ai/vector_store.py` — ONNX 로컬 임베딩 (API 키 불필요)
- [x] `bot/telegram_bot.py` — `/alert`, `/bookmark` 명령어 추가

---

## 진행 중인 작업 🔄

없음 — Phase 2 전체 완료.

---

## 남아 있는 작업 📋 (Phase 3 후보)

| 우선순위 | 작업 | 예상 난이도 | 예상 효과 |
|----------|------|-------------|-----------|
| ⭐⭐⭐ | AI 팟캐스트 (TTS 요약 오디오) | 상 | 차별화 기능 |
| ⭐⭐⭐ | Knowledge Graph 시각화 | 상 | 온톨로지 연결 시각화 |
| ⭐⭐ | 다국어 지원 (EN/JP/ZH) | 중 | 글로벌 확장 |
| ⭐⭐ | 모바일 반응형 UI 개선 | 중 | 접근성 향상 |
| ⭐ | PR 생성 + v1.4.0 Release | 하 | 배포 완료 |

---

## Git 상태 (v1.4.0)

| 커밋 | 내용 |
|------|------|
| `978513b` | feat: Discord 봇 추가 |
| `cb3632e` | feat: GitHub Actions 워크플로우 추가 |
| `3f69f06` | test: 통합 검증 완료 |
| `8e77c56` | feat: app.py SQLite 연동 + UI/UX 개선 |
| `049d22f` | feat: bookmarks + telegram_bot DB 전환 |
| `c4f0ddf` | feat: batch_processor + chat + smart_alert DB 전환 |
| `df55b69` | feat: crawler DB 전환 |
| `ad45b28` | feat: 마이그레이션 스크립트 + .gitignore |
| `80f1928` | feat: db 패키지 + SQLite CRUD/FTS5/WAL |

**브랜치:** `feat/v1.4.0-sqlite-discord-actions` (push 필요)

---

## 변경된 파일 (v1.4.0)

| 파일 | 작업 | 핵심 변경 |
|------|------|-----------|
| `db/__init__.py` | **신규** | 28개 DB 함수 re-export |
| `db/database.py` | **신규** | SQLite WAL+FTS5 전체 CRUD |
| `utils/migrate_json_to_sqlite.py` | **신규** | JSON → SQLite 1회성 마이그레이션 |
| `bot/discord_bot.py` | **신규** | Discord 봇 5개 명령어 |
| `.github/workflows/crawl.yml` | **신규** | 매시간 자동 수집 |
| `config.py` | 수정 | DB_PATH 추가 |
| `app.py` | 수정 | DB 연동 + UI/UX 개선 |
| `crawler/rss_crawler.py` | 수정 | DB 전환 |
| `ai/batch_processor.py` | 수정 | DB 전환 |
| `ai/chat.py` | 수정 | FTS5 검색 연동 |
| `ai/smart_alert.py` | 수정 | DB 전환 |
| `utils/bookmarks.py` | 수정 | DB wrapper |
| `bot/telegram_bot.py` | 수정 | DB 전환 |
| `requirements.txt` | 수정 | discord.py 추가 |
| `.gitignore` | 수정 | radar.db 추가 |

---

## 이어하기용 명령어

### 현재 상태 확인
```bash
cd "D:/AI_Dev_Work/2026y/26y_03m_22d_ai-news-radar/ai-news-radar"
git branch && git log --oneline -5
```

### 앱 실행
```bash
cd "D:/AI_Dev_Work/2026y/26y_03m_22d_ai-news-radar/ai-news-radar"
.venv/Scripts/streamlit run app.py --server.port 6601
```

### 텔레그램 봇 실행
```bash
.venv/Scripts/python -m bot.telegram_bot
```

### Discord 봇 실행
```bash
.venv/Scripts/python -m bot.discord_bot
```

### DB 상태 확인
```bash
.venv/Scripts/python -c "
from db.database import init_db, get_article_count, get_processed_count
init_db()
print('기사 총:', get_article_count(), '| AI처리:', get_processed_count())
"
```

### 마이그레이션 (신규 설치 시 1회)
```bash
.venv/Scripts/python -m utils.migrate_json_to_sqlite
```

### 보안 스캔
```bash
grep -rn "AIza\|sk-\|ghp_\|xoxb-\|AKIA" db/ utils/ bot/ ai/ app.py 2>/dev/null && echo "경고!" || echo "안전"
```

---

## 발생한 문제 / 해결

| 문제 | 해결 |
|------|------|
| chromadb DefaultEmbeddingFunction → Google API 호출 | ONNXMiniLM_L6_V2 로컬 모델로 교체 ✅ |
| 5MB articles.json 매 요청마다 로드 → 느림 | SQLite WAL + st.cache_data(ttl=60) ✅ |
| Streamlit 멀티스레드 SQLite 충돌 | thread-local 커넥션 패턴 적용 ✅ |

---

## 남아 있는 이슈

- GitHub Actions: `data/radar.db` Git 커밋 방식은 실험적 — 장기적으로 별도 persistence 검토
- `google.generativeai` FutureWarning: chromadb import 시 발생, 동작 무관
- Discord 봇: `.env`에 `DISCORD_BOT_TOKEN` + `DISCORD_CHANNEL_ID` 설정 필요

---

## 체크포인트

```
[v1.4.0] 2026-04-17 완료
- SQLite WAL+FTS5 전환 완료 (3,234기사 마이그레이션)
- 전체 모듈 DB 연동 (app.py, crawler, ai/*, bot/*, utils/*)
- GitHub Actions 자동 수집 워크플로우 추가
- Discord 봇 추가 (top/today/search/alert/stats)
- 보안 검증: SQL Injection PASS, 동시성 PASS, FTS5 0.3ms, 비밀정보 없음
- Branch: feat/v1.4.0-sqlite-discord-actions
```
