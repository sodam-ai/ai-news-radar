# .resume.md — AI News Radar 작업 이어하기 기록

> **마지막 업데이트:** 2026-04-19
> **최신 릴리스:** v1.6.1 (2026-04-19)
> **main HEAD:** `96207ab docs(resume)` → 본 커밋 후 갱신 예정
> **용도:** Claude Code compact 후 또는 세션 재시작 시 맥락 완전 복원

---

## 🎯 다음 세션 시작 프롬프트 (복사·붙여넣기)

```
AI News Radar 프로젝트 이어서 작업.
경로: D:/AI_Dev_Work/2026y/26y_03m_22d_ai-news-radar/ai-news-radar

.resume.md 를 먼저 읽고, 거기에 명시된
"다음 최우선 작업 = P5 UI/UX 업그레이드(2~3h)"
를 진행하기 위한 상세 플랜을 수립해서 보여줘.
단, 내가 승인하기 전에는 파일 수정 금지.
```

---

## 📁 프로젝트 경로

| 항목 | 경로 |
|------|------|
| 로컬 절대 경로 | `D:\AI_Dev_Work\2026y\26y_03m_22d_ai-news-radar\ai-news-radar\` |
| GitHub | https://github.com/sodam-ai/ai-news-radar |
| 원격 브랜치 | `main` (기본) |
| Streamlit 포트 | 6601 → http://localhost:6601 |
| Python 가상환경 | `.venv/` (Python 3.13) |

---

## 🗂️ 관련 폴더 / 파일 위치

### 핵심 애플리케이션
| 파일 | 역할 | 주요 위치 |
|------|------|-----------|
| `app.py` (~2,350줄) | Streamlit 메인 대시보드 | 전체 |
| `app.py` | 사이드바 (⚙️ 설정 버튼) | L~380 |
| `app.py` | 6탭 + 온보딩 웰컴 카드 | L~625 |
| `app.py` | `@st.dialog show_settings_dialog()` | L~1475 |
| `app.py` | `📖 사용 가이드` sec_guide | L~2145 |
| `app.py` | `_open_settings_dialog` 플래그 체크 | 파일 끝 |
| `config.py:17` | `load_dotenv(override=True)` | 필수 |
| `ai/model_router.py` | `PROVIDERS` dict (39개) | L~66 |
| `ai/model_router.py` | `get_active_provider()` | L~505 |
| `utils/env_writer.py` | 원자적 `.env` R/W | 전체 |
| `utils/security.py` | SSRF / 경로 / 마스킹 / CSPRNG | 전체 |
| `db/database.py` | SQLite WAL + FTS5 (28 공개 함수) | 전체 |

### GitHub Actions
| 파일 | 역할 |
|------|------|
| `.github/workflows/crawl.yml` | 매시간 RSS 수집 (🟢 정상) |
| `.github/workflows/collect.yml` | 매일 3회 AI 처리 (⏭️ graceful skip) |
| `.github/workflows/ci.yml` | pytest + pip-audit + detect-secrets |

### 테스트 (159 passed)
| 파일 | 역할 |
|------|------|
| `tests/test_env_writer.py` | 원자적 write 18 테스트 |
| `tests/test_security.py` | SSRF/경로/마스킹 검증 |
| `tests/test_db_crud.py` | SQLite CRUD |
| `tests/test_fts5_search.py` | Full-text search |
| `tests/test_knowledge_graph.py` | 엔티티·관계 |
| `tests/test_crawler_ssrf.py` | 크롤러 차단 |
| `tests/test_regression.py` | 회귀 방지 |

### 문서
| 파일 | 역할 |
|------|------|
| `README.md` | 한국어 기본 README (v1.6.0 반영) |
| `README_KO.md` | 한국어 상세 |
| `README_EN.md` | 영어 상세 |
| `SECURITY.md` | OWASP ASVS L1 상태 |
| `.PRD/` | PRD 정본 (숨김 폴더) |
| `.resume.md` | 본 파일 |

---

## 📍 현재 작업 위치

- **브랜치:** `main`
- **HEAD:** 본 커밋 직전 `96207ab` → 커밋 후 새 해시
- **단계:** v1.6.1 릴리스 완료 · 다음 P5 UI/UX 대기
- **열린 PR:** 없음

---

## ✅ 완료된 작업 (이번 세션 2026-04-17 ~ 2026-04-19)

### 머지된 PR (4건)
| PR | 제목 | 커밋 |
|----|------|------|
| #6 | pytest 회귀 스위트 + CI 게이트 | `e123c62` |
| #8 | ⚙️ 설정 모달 + 로컬 LLM 4종 + 클라우드 20 프로바이더 | `ceb389e` |
| #9 | Phase 2b + 3 — SMTP/SNS 이전 + 온보딩 웰컴 카드 | `784d99f` |
| #10 | Actions graceful skip + 앱 내 상태·📖 가이드 + README 리프레시 | `55fc6dc` |

### 릴리스 (2건)
- `v1.6.0` — 설정 통합 (사이드바 버튼 + 8섹션 모달)
- `v1.6.1` — 운영 개선 (graceful skip + 가이드 + README)

### 보안 강화 (OWASP ASVS L1 기준선)
- V5.5.2 SSRF 차단 (`safe_url`)
- V12.3.1 경로 탈출 차단 (`safe_join`)
- V7.1.1 로그 마스킹 (`mask_secret`)
- V14.4.1 시크릿 스캔 (pre-commit)
- V10.3.2 의존성 CVE 0건
- V5.3.4 SQL Injection 방지
- V6.2.1 CSPRNG
- V13.2.1 HTTP timeout

### 테스트 지표
- pytest: 141 → **159 passed**
- 커버리지: `utils/security` 92%, `db/database` 96%

---

## 🔄 진행 중인 작업

**없음.** v1.6.1 릴리스 후 깨끗한 상태.

본 커밋은 **보안 하드닝 + 다음 세션 준비**:
- `.resume.md` 실제 키 문자열 마스킹 (노출 방지)
- `.gitignore` 로컬 메모 파일 패턴 추가

---

## 📋 남아 있는 작업 (우선순위 순)

### 🔴 P3 — 사용자 액션 (5분, 사용자 직접 수행)
1. https://aistudio.google.com/apikey — 새 Gemini 키 발급
2. 기존 키(만료 + 채팅 노출) 폐기
3. 앱 ⚙️ 설정 → 🔑 LLM API 키 → 저장
4. ⚙️ 설정 → ☁️ 클라우드 동기화 → 📤 동기화
5. ▶️ `collect.yml` 수동 실행 → 확인

### 🏆 P5 — UI/UX 업그레이드 (**다음 최우선**, 2~3h)
- **목표:** Carbon Design + Fluent 2 + WCAG 2.2 AA
- **산출물:**
  - `assets/theme.toml` — Carbon Blue `#0F62FE`
  - 폰트: Inter + JetBrains Mono
  - 색 대비 4.5:1 (Lighthouse ≥ 90)
  - `st.status()` + `st.toast()` 표준화
- **검증:** pytest 159+ 유지 + chrome-devtools-mcp 스크린샷

### 🟡 P4 — Turso DB 전환 (별도 세션, 3~5h)
- `data/radar.db` Git 자동 커밋 제거 → libsql 원격 DB
- `db/database.py` connection 추상화
- 3,585+ 행 마이그레이션
- `crawl.yml` `contents: write` → `contents: read` (V14.2.1 완결)

### 🟢 P6 — PRD 미구현 기능 1건 (가변)
후보: 팟캐스트 TTS · 경쟁사 모니터링 UI · 주간 PDF 자동 배포 · 멀티 에이전트 토론

### 🟢 작은 정리 (각 1~30분)
- `PRD/` 중복 폴더 제거
- 로컬 feat 브랜치 정리
- Dependabot 활성화
- `google-generativeai` → `google-genai` 마이그 (deprecation)

---

## 🏆 다음 최우선 작업 — P5 UI/UX 업그레이드

### 왜 P5가 최우선인가
1. **체감 가치 최대** — 모든 화면에 즉시 반영되는 시각 개선
2. **회귀 위험 최소** — pytest 159 + chrome-devtools-mcp 이중 검증
3. **비개발자 친화** — 색/폰트 개선만으로도 "더 깔끔해졌다" 체감
4. **P4 대비** — Turso는 3~5h 대형이지만 즉시 체감 낮음
5. **원칙 일관성** — "초보자 친화" 원칙과 맞음

### P5 실행 단계 (다음 세션)
1. 브랜치 생성: `git checkout -b feat/ui-carbon-fluent`
2. `assets/theme.toml` 신규 — Carbon Blue `#0F62FE`
3. `.streamlit/config.toml` 테마 로드 경로
4. Inter + JetBrains Mono 웹폰트 로드
5. 주요 st.button/st.metric 색 대비 4.5:1 확보
6. `st.status()`/`st.toast()`로 상태 피드백 표준화
7. chrome-devtools-mcp Lighthouse ≥ 90 확인
8. pytest 회귀 159+ 검증
9. PR 생성 → CI 녹색 → 머지 → `v1.7.0` 릴리스

---

## 📝 변경된 파일 (본 커밋)

| 파일 | 변경 내용 |
|------|-----------|
| `.resume.md` | 전체 재작성 (실제 키 마스킹 + P5 최우선 명시 + 표준 13섹션) |
| `.gitignore` | 로컬 메모 패턴 추가 (`기존 작업 이어서 진행 명령어.md`, `*.memo.md`) |

---

## 🔧 핵심 수정 내용

1. **보안 하드닝:** 이전 `.resume.md` L202의 실제 (만료) Gemini 키 문자열 `AIzaSy*`를 **마스킹**하여 문서 공개 시에도 노출 표면 0.
2. **다음 작업 명시:** 사용자 지시에 따라 "다음 최우선 작업 = P5 UI/UX"를 여러 섹션에 반복 기재.
3. **표준 13섹션:** 프로젝트 경로 → 폴더/파일 → 현재 위치 → 완료 → 진행 → 남은 작업 → 최우선 → 변경 파일 → 핵심 수정 → 문제/원인/해결 → 남은 이슈 → 체크포인트 → 이어하기 명령어.
4. **이어하기 명령어 강화:** 복사 즉시 실행 가능한 CLI 블록 제공.

---

## 🚨 발생한 문제 / 원인 추정 / 해결 여부

| 문제 | 원인 | 해결 | 상태 |
|------|------|:---:|:---:|
| 이전 `.resume.md`에 실제 Gemini 키 노출 | 디버그 기록을 그대로 보존 | 본 커밋에서 마스킹 | ✅ 해결 |
| `기존 작업 이어서 진행 명령어.md` untracked 잔류 | `.gitignore` 미포함 | 패턴 추가 | ✅ 해결 |
| `.env` Gemini 키 401/만료 | 테스트 키 자체가 만료 | 사용자 직접 재발급 예정 (P3) | 🟡 대기 |
| `AIzaSyB...` README 예시 값 | 문서용 플레이스홀더 (`xxxxx`) | 의도된 가짜 | ✅ 무해 |
| Windows ripgrep 미설치 → Grep 도구 실패 | 환경 문제 | `git ls-files \| xargs grep`으로 우회 | ✅ 대체 |

---

## ⚠️ 남아 있는 이슈

- 🔴 **[P3]** 새 Gemini 키 발급·등록 — 사용자 직접 수행
- 🟡 **[P4]** `data/radar.db` Git 자동 커밋 — Turso 전환 시 해소
- 🟡 **[P4]** `crawl.yml` `contents: write` 권한 — Turso 전환 시 강등
- 🟢 **[운영]** `PRD/` 중복 폴더 — 원본은 `.PRD/`
- 🟢 **[운영]** 로컬 feat 브랜치 잔재 정리
- 🟢 **[의존성]** `google-generativeai` → `google-genai` deprecation 마이그

---

## 📌 체크포인트

### ✅ 보안 점검 (본 커밋 직전)
- [x] `.env` `.gitignore:2` 등록 확인
- [x] 추적 파일 중 API 키 패턴 스캔 (`git ls-files | xargs grep`)
- [x] `.resume.md` 실제 키 마스킹
- [x] `README_KO.md:154` 예시 값(`xxxxx`) 유해성 없음 확인
- [x] `tests/test_security.py` 테스트 픽스처 안전 확인
- [x] 로컬 메모 파일 `.gitignore` 추가

### ✅ 코드베이스 상태
- [x] main 원격 동기화 (`git pull origin main`)
- [x] 열린 PR 0건
- [x] CI 녹색
- [x] pytest 159 passed

### ⏭️ 다음 세션 선결 조건
- [ ] (선택) P3 사용자 액션 — 새 Gemini 키 발급
- [ ] (필수) 본 커밋 + push 확인
- [ ] (선택) P5 사전 조사 — Streamlit theme.toml 최신 문법

---

## 🚀 이어하기용 명령어 (복사 즉시 실행)

### ① 프로젝트 상태 복구 (어느 기기에서든)
```bash
cd "D:/AI_Dev_Work/2026y/26y_03m_22d_ai-news-radar/ai-news-radar"
git fetch --all --tags
git pull origin main
git branch --show-current
git log --oneline -5
```

### ② 로컬 검증
```bash
.venv/Scripts/python -m pytest tests/ -q
.venv/Scripts/python -c "from ai.model_router import PROVIDERS; print(f'프로바이더 {len(PROVIDERS)}개')"
```

### ③ Streamlit 실행
```bash
.venv/Scripts/streamlit run app.py --server.port 6601 --server.headless true
```
→ http://localhost:6601

### ④ Actions 상태 확인
```bash
gh run list --workflow collect.yml -L 5
gh run list --workflow crawl.yml -L 5
gh release list -L 3
```

### ⑤ P5 UI/UX 작업 시작 (다음 세션 권장 순서)
```bash
# 1. 작업 브랜치 생성
git checkout -b feat/ui-carbon-fluent

# 2. 테마 파일 신규 생성 (작업 중)
# assets/theme.toml + .streamlit/config.toml

# 3. 검증
.venv/Scripts/python -m pytest tests/ -q

# 4. 커밋 (진행 중간마다 누적)
git add .
git commit -m "feat(ui): Carbon Blue 테마 초안"

# 5. push
git push -u origin feat/ui-carbon-fluent

# 6. (최종 검토 후) PR 생성 — 자동 금지
```

### ⑥ 긴급 롤백
```bash
git fetch --tags
git checkout v1.6.1              # 안정 릴리스
git reset --hard origin/main     # 원격 main 상태로 복귀
```

---

## 🧭 운영 원칙 (유지)

- **하드코딩 금지** — PROVIDERS dict 동적 참조, env var 오버라이드
- **보안 최우선** — `.env` gitignore + password 필드 + atomic write
- **신뢰 도구만** — chrome-devtools-mcp / context7 / 내장 도구
- **단일 저장 경로** — 모든 키 저장은 `utils/env_writer.update_env` 경유
- **graceful 패턴** — 키 없음은 실패 아닌 스킵
- **초보자 친화** — 기술 용어 최소화, 앱 내 가이드
- **자동 merge/release 금지** — 최종 반영 시에만 사용자 검토 후

---

**본 파일은 GitHub main 브랜치에 커밋되어 있어 어느 기기에서든 `git pull` 후 동일하게 이어집니다.**
