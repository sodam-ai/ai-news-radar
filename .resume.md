# .resume.md — AI News Radar 작업 이어하기 기록

> **마지막 업데이트:** 2026-04-19
> **최신 릴리스:** v1.6.1
> **main HEAD:** `55fc6dc`
> **용도:** Claude Code compact 후 또는 세션 재시작 시 맥락 복원

---

## 🎯 Compact 후 이어가는 방법 — 붙여넣기용 프롬프트

compact 실행 직후 아래 텍스트를 붙여넣으면 Claude가 `.resume.md` 읽고 맥락을 완전 복원합니다.

```
AI News Radar 프로젝트 이어서 작업.
경로: D:/AI_Dev_Work/2026y/26y_03m_22d_ai-news-radar/ai-news-radar
현재 상태: v1.6.1 릴리스 완료 (main 55fc6dc).
이전 세션 맥락은 `.resume.md` 참조.
먼저 현재 git 상태와 다음 작업 후보 섹션을 확인한 뒤,
어떤 작업을 진행할지 강력 추천안을 알려줘.
```

---

## 📊 현재 상태 스냅샷 (2026-04-19)

### GitHub 저장소
- **URL:** https://github.com/sodam-ai/ai-news-radar
- **main HEAD:** `55fc6dc feat(actions+guide): graceful skip + 앱 내 Actions 상태·📖 가이드 + README 리프레시 (#10)`
- **최근 커밋 5개:**
  ```
  55fc6dc feat(actions+guide): graceful skip + 앱 내 상태·📖 가이드 + README 리프레시 (#10)
  5499084 docs: 릴리스 배지 v1.5.0 → v1.6.0 (설정 모달·로컬 LLM·온보딩 통합)
  784d99f feat(settings): Phase 2b + 3 — SMTP/SNS 이전 + 온보딩 웰컴 카드 (#9)
  ceb389e feat(ui): ⚙️ 설정 모달 + 로컬 LLM 4종 + 클라우드 20 프로바이더 확대 (#8)
  e123c62 test: pytest 회귀 스위트 + CI 게이트 (보안 방어선 증명) (#6)
  ```

### 릴리스 태그
- `v1.6.1` — 2026-04-19 (Actions 이메일 해결 + 가이드)
- `v1.6.0` — 2026-04-19 (설정 통합)
- `v1.5.0` — 2026-04-17 (Knowledge Graph)

### 로컬 환경
- **경로:** `D:\AI_Dev_Work\2026y\26y_03m_22d_ai-news-radar\ai-news-radar\`
- **브랜치:** `main` (원격 동기화 완료)
- **Python:** 3.13 (venv: `.venv/`)
- **Streamlit 포트:** 6601

---

## 🚀 이어하기 명령어 (그대로 복사·실행)

### ① 프로젝트 상태 복구
```bash
cd "D:/AI_Dev_Work/2026y/26y_03m_22d_ai-news-radar/ai-news-radar"
git fetch --all --tags
git branch --show-current
git log --oneline -5
```

### ② 원격과 동기화
```bash
git pull origin main
```

### ③ 로컬 검증 (159 passed 기대)
```bash
.venv/Scripts/python -m pytest tests/ -q
.venv/Scripts/python -c "from ai.model_router import PROVIDERS; print(f'프로바이더 {len(PROVIDERS)}개')"
```

### ④ Streamlit 실행
```bash
.venv/Scripts/streamlit run app.py --server.port 6601 --server.headless true
```
→ http://localhost:6601

### ⑤ Actions 상태 확인
```bash
gh pr list                              # 열린 PR 없어야 정상
gh run list --workflow collect.yml -L 5 # graceful skip으로 ✅ 기대
gh run list --workflow crawl.yml -L 5   # RSS 수집 ✅ 기대
gh release list -L 3                    # v1.6.1, v1.6.0, v1.5.0
```

---

## 🎉 이번 세션 완료 작업 (2026-04-17 ~ 2026-04-19)

### 머지된 PR (4건)
| PR | 제목 | 커밋 |
|----|------|------|
| #6 | pytest 회귀 스위트 + CI 게이트 | `e123c62` |
| #8 | ⚙️ 설정 모달 + 로컬 LLM 4종 + 클라우드 20 프로바이더 | `ceb389e` |
| #9 | Phase 2b + 3 — SMTP/SNS 이전 + 온보딩 웰컴 카드 | `784d99f` |
| #10 | Actions graceful skip + 앱 내 상태·📖 가이드 + README 리프레시 | `55fc6dc` |

### 릴리스 2건
- **v1.6.0** — 설정 통합 (사이드바 버튼 + 8섹션 모달)
- **v1.6.1** — 운영 개선 (graceful skip + 가이드 + README)

### 주요 변경
- `utils/env_writer.py` 신규 — 원자적 `.env` R/W + 18 회귀 테스트
- `utils/security.py` — SSRF/경로/마스킹/CSPRNG
- `ai/model_router.py` — 로컬 LLM 4종 추가 → 39 프로바이더 (Ollama/LM Studio/llama.cpp/Jan)
- `config.py` — `load_dotenv(override=True)`
- `.github/workflows/collect.yml` — graceful skip 스텝
- `app.py` — 설정 모달 8섹션 + 사이드바 버튼 + 온보딩 카드 + Actions 상태 + 📖 가이드
- README 3종 v1.6.0 리프레시 (배지·기능·설치 가이드)

### 테스트 지표
- pytest: 141 → **159 passed**
- 커버리지: utils/security 92%, db/database 96%

---

## 🔍 현재 구조 (v1.6.1)

### 탭 6개 + 온보딩 카드
- 🏠 대시보드 / 📰 뉴스피드 / 💬 AI / 📊 인사이트 / 📢 공유 / 🕸️ 그래프
- **온보딩 웰컴 카드:** `_active_provider` 없을 때 최상단 자동 표시

### ⚙️ 설정 모달 (사이드바 프라이머리 버튼, 8섹션)
| 탭 | 역할 |
|----|------|
| 🔑 LLM API 키 | 20 프로바이더 드롭다운 (로컬/무료/유료) |
| ⏱️ 크롤링 | 주기 슬라이더 + 수동 실행 |
| 📰 뉴스 소스 | on/off 토글 |
| 📧 이메일 | SMTP + 뉴스레터 테스트 |
| 📢 SNS | Discord/Telegram/X/Threads/Instagram |
| ☁️ 클라우드 동기화 | GitHub Secrets 일괄 동기화 + 워크플로우 트리거 |
| ℹ️ 시스템 정보 | 버전·통계·Actions 최근 10회 상태 |
| 📖 사용 가이드 | 완전 초보자용 9개 expander |

---

## ⚙️ GitHub Actions 상태

| 워크플로우 | 주기 | 상태 | Secrets |
|-----------|------|:---:|:---:|
| `crawl.yml` | 매시간 | 🟢 정상 성공 | ❌ |
| `collect.yml` | 매일 3회 | ⏭️ graceful skip (키 없으면) | ✅ LLM 키 |
| `ci.yml` | push/PR | 🟢 녹색 | ❌ |

**graceful skip:** `collect.yml`이 Secrets에 LLM 키 없으면 실패 대신 성공(스킵) 종료 → GitHub 이메일 알림 차단

---

## 🔒 보안 기준선 (OWASP ASVS L1)

**완료:**
- V5.5.2 SSRF 차단 (`safe_url`)
- V12.3.1 경로 탈출 차단 (`safe_join`)
- V7.1.1 로그 마스킹 (`mask_secret`)
- V14.4.1 시크릿 스캔 (pre-commit)
- V10.3.2 의존성 CVE 0건
- V5.3.4 SQL Injection 방지
- V6.2.1 CSPRNG (random → secrets)
- V13.2.1 HTTP timeout

**남음:** V14.2.1 Actions `contents: write` → `contents: read` (Turso 전환 시 해소)

---

## 📋 다음 작업 후보 (우선순위 순)

### 🔴 P3 — 사용자 액션 (5분, 자동화 복구)
1. https://aistudio.google.com/apikey — 새 Gemini 키 발급
2. 기존 `AIzaSyAMyr...` 키 폐기 (채팅에 노출됨 — 반드시 삭제)
3. 앱 ⚙️ 설정 → 🔑 LLM API 키 → 저장
4. ⚙️ 설정 → ☁️ 클라우드 동기화 → 📤 동기화
5. ▶️ `collect.yml` 수동 실행 → 확인

### 🟡 P4 — Turso DB 전환 (3~5h)
- `data/radar.db` Git 자동 커밋 제거 → libsql 원격 DB
- `db/database.py` connection 추상화
- 3,585+ 행 마이그레이션
- `crawl.yml` `contents: write` → `contents: read` (V14.2.1 완결)

### 🟡 P5 — UI/UX 업그레이드 (2~3h)
- Carbon Design + Fluent 2 + WCAG 2.2 AA
- `assets/theme.toml` — Carbon Blue `#0F62FE`
- Inter + JetBrains Mono
- 색 대비 4.5:1 (Lighthouse ≥ 90)
- `st.status()` + `st.toast()` 표준화

### 🟢 P6 — PRD 미구현 기능 1건 (가변)
후보: 팟캐스트 TTS / 경쟁사 모니터링 / 주간 PDF 자동 배포 / 멀티 에이전트 토론 / 뉴스레터 자동 발송

### 🟢 작은 정리 (각 1~30분)
- `PRD/` 중복 폴더 제거
- 로컬 feat 브랜치 정리 (`git branch | grep feat/`)
- Dependabot 활성화 (`.github/dependabot.yml`)
- `google-generativeai` → `google-genai` 마이그 (deprecation)
- v1.4.0 태그 소급

---

## ⚠️ 알려진 이슈 / 주의사항

- `.env`의 Gemini 키 `AIzaSyAMyrSmuUf2uzd6yKYcPgGW5QJSSYKTH2s`는 **만료된 테스트 키** — 폐기 필수
- OpenAI 키 `sk-proj-...`는 401 Unauthorized — 사용 불가
- GitHub Secrets 비어있음 — `collect.yml`은 graceful skip으로 정상
- 로컬 memo 파일 `기존 작업 이어서 진행 명령어.md` untracked (Git 제외)
- 사용자 프로필: **비개발자·완전 초보자** — 기술 용어 최소화, 앱 UI 중심 안내
- `.env`는 `.gitignore` 등록됨 — GitHub 커밋 절대 금지

---

## 🗂️ 핵심 파일 위치 맵

| 파일 | 역할 |
|------|------|
| `app.py` (~2,350줄) | Streamlit 메인 대시보드 |
| `app.py` L~380 | 사이드바 (⚙️ 설정 버튼 + 액션 버튼) |
| `app.py` L~625 | 6탭 배열 + 온보딩 웰컴 카드 |
| `app.py` L~1475 | `@st.dialog show_settings_dialog()` 시작 |
| `app.py` L~2145 | `📖 사용 가이드` sec_guide 블록 |
| `app.py` 파일 끝 | `_open_settings_dialog` 플래그 체크 |
| `ai/model_router.py` L~66 | `PROVIDERS` dict (39개) |
| `ai/model_router.py` L~505 | `get_active_provider()` |
| `utils/env_writer.py` | 원자적 `.env` R/W |
| `utils/security.py` | SSRF / 경로 / 마스킹 / CSPRNG |
| `db/database.py` | SQLite WAL + FTS5 (28 공개 함수) |
| `config.py:17` | `load_dotenv(override=True)` |
| `.github/workflows/collect.yml` | graceful skip 적용 |
| `.github/workflows/crawl.yml` | 매시간 RSS 수집 |
| `.github/workflows/ci.yml` | pytest + pip-audit + detect-secrets |
| `tests/` | 7개 파일 (159 passed) |

---

## 🎬 권장 다음 세션 흐름

```
1. Compact 완료 확인
2. 위 "Compact 후 이어가는 방법" 프롬프트 붙여넣기
3. Claude가 `.resume.md` 읽고 현재 상태 인지
4. "어느 P 작업으로 진행할까?" 질문에 답
5. 승인된 플랜으로 작업 착수
```

---

## 📑 참고 — 이번 세션 주요 대화 맥락

### 주요 사용자 피드백
1. "설정, 세팅, api 키 등 설정 하는게 너무 어렵고 UI에서 잘 눈에 띄지 않음" → 사이드바 프라이머리 버튼 + 모달
2. "항상 보여야 하는게 내가 원하는거" → 8섹션 모달 + 상태 뱃지
3. "LLM API 키 더욱 더 많은 범위로 확대. 로컬 LLM 도 범위 확대" → 로컬 4종 + 20 프로바이더
4. "크롤링은 잘 되고 있으나 이메일 알람이 너무 많이 와서 신경쓰임" → graceful skip
5. "비개발자, 완전 초보자라 까먹으면 할 줄 모름" → 📖 사용 가이드 탭
6. "README 도 기능에 맞춰서 새로 작성" → README 3종 리프레시

### 확립된 작업 원칙
- **하드코딩 금지:** PROVIDERS dict 동적 참조, env var 오버라이드
- **보안 최우선:** `.env` gitignore + password + atomic write
- **신뢰 도구만:** chrome-devtools-mcp / context7 / 내장 도구
- **단일 저장 경로:** 모든 키 저장은 `update_env` 경유
- **graceful 패턴:** 키 없음은 실패 아닌 스킵
- **초보자 친화:** 기술 용어 최소화, 앱 내 가이드

---

## 🛟 긴급 롤백 명령 (문제 발생 시)

```bash
cd "D:/AI_Dev_Work/2026y/26y_03m_22d_ai-news-radar/ai-news-radar"
git fetch --tags

# 최신 안정 릴리스로 복귀
git checkout v1.6.1

# 이전 안정 버전들
git checkout v1.6.0
git checkout v1.5.0

# 로컬 변경 버리고 원격 main 상태로
git reset --hard origin/main
```

---

**이 파일은 GitHub main 브랜치에 커밋되어 있어 원격 저장소에서도 접근 가능합니다.**
